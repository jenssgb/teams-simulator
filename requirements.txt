# Core runtime dependencies for the Teams Simulator.
# Version ranges chosen to have prebuilt wheels for Python 3.10 - 3.13 on Windows.

sounddevice>=0.4.7,<0.6
soundfile>=0.12.1,<0.14
pydub>=0.25.1,<0.26
# Python 3.13 removed the stdlib `audioop` module; pydub still imports it.
audioop-lts>=0.2.1; python_version >= "3.13"
numpy>=2.1,<3
scipy>=1.14,<2
opencv-python>=4.10,<5
pyvirtualcam>=0.11,<1
